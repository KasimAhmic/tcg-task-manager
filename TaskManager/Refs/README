Run the scripts/link.ps1 to populate this directory with symlinks, hard links, or copies of the files needed to build
the project. If the script is ran in elevated mode, it will create symlinks. It will fallback to hard links if not
elevated and will then fallback to copies if hard links fail.
